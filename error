# error default index if autoindex off, shows an empty page instead of 404
# error Connection: close || keep-alive empty
# error when the client send more than 10 bytes (empty response) but the server knows it \
(curl -X POST -H "Content-Type: plain/text" localhost:8080 -d "body is here")
# error setup multiple routes for multiple directories (when i hit /route1 i should se and browse what inside the direcroty1) see location /route1
# error if a method not allowed it should throw an error method not allowed


# solved issue
# close connection
# response non block io

server
[
    host: 0.0.0.0
    port: 8080
    root: www

    error_page:404:www/notfound.html
    client_max_body_size: 10
    location: /
    {
        root= www
        allowed_methods=GET, POST
        autoindex=on
    }
]